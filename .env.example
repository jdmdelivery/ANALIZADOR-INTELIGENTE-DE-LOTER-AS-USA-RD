# Copiar a .env y ajustar valores (no subir .env a git)

FLASK_ENV=development
# FLASK_ENV=production
# PRODUCTION=1

SECRET_KEY=cambiar-por-clave-larga-y-aleatoria-min-32-chars

DATABASE_PATH=lottery.db

# Solo primer arranque (cuando la DB está vacía)
INITIAL_ADMIN_USERNAME=jdmcashnow
INITIAL_ADMIN_PASSWORD=cambiar-password-seguro

HOST=127.0.0.1
PORT=5000

LOG_LEVEL=INFO

# Scrapers USA (Render: subir timeout si hay bloqueos Cloudflare)
# USA_FETCH_TIMEOUT=35
# USA_FETCH_RETRIES=4

# LEIDSA (opcional)
# LEIDSA_TEST_MODE=0
